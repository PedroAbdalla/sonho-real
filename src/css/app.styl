// app global css
body {
    font-family: 'Open Sans', sans-serif;
    a {
        text-decoration: none;
        &:hover {
            text-decoration: underline;
        }
    }
    #q-app {
        height: 100%;
    }
}
